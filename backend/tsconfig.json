{
    "compilerOptions": {
        "module": "commonjs",
        "target": "ES2020",
        // "outDir": "dist", // this will output all JS files when running 'tsc' command
        // "rootDir": "src", // this sets the root location of where 'tsc' should look to compile JS files
        "resolveJsonModule": true
    },
    "exclude": [
        "node_modules"
    ],
}